SELECT 	REC.NOME AS RECEITA,
		ING.NOME AS INGREDIENTE,
		RECING.QTD,
		RECING.UNIDADE_MEDIDA
		
FROM 	receita REC, 
		ingrediente ING, 
		receita_ingredientes RECING 
		
WHERE 	REC.ID = RECING.RECEITA_ID 
		AND ING.ID = RECING.INGREDIENTE_ID
		AND REC.ID = 1;